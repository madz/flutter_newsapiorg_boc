name: flutter_newsapiorg
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.0+3
  provider: ^4.0.1
  json_annotation: ^3.0.1
  flutter_pagewise: ^1.2.3
  webview_flutter: ^0.3.18+1
  flutter_bloc: ^3.0.0
  equatable: ^1.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.7.2
  json_serializable: ^3.2.5

flutter:

  uses-material-design: true
  assets:
     - images/
